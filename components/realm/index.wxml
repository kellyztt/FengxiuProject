<!--components/realm/index.wxml-->
<view class="container">
    <view>
        <image></image>
    </view>
    <block wx:for="{{fences}}" wx:key="{{index}}">
        <s-fence data-x="{{index}}" fence="{{item}}" bind:cellTap="onCellTap"></s-fence>
    </block>

</view>
