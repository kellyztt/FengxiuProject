<!--components/fence/index.wxml-->
<view class="container">
    <text class="title">{{fence.title}}</text>
    <view class="row-container">
        <block wx:for="{{fence.cells}}" wx:key="index">
            <s-cell x="{{x}}" y="{{index}}" class="cell" cell="{{item}}"></s-cell>
        </block>
        <view class="hr"></view>
    </view>
    
</view>
