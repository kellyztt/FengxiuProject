<view class="container">
    <block wx:for="{{_texts}}" wx:key="{{index}}">
        <view class="inner">
            <l-icon name="dot" l-class="sleeve" size="28"></l-icon>
            <text>{{item}}</text>
        </view>
    </block>
</view>
