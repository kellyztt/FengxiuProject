<!--pages/cart/cart.wxml-->
<view wx:if="{{!isEmpty}}" class="container">
    <block wx:for="{{cartItems}}" wx:key="index">
        <s-cart-item cart-item="{{item}}" bind:itemdelete="onDelete"></s-cart-item> 
    </block>
</view>
